import { createContext } from "react";

export const AppContext = createContext(null);
export const ProfileContext = createContext(null);
export const UsersContext = createContext(null);
export const ChatsContext = createContext(null);
export const RequestsContext = createContext(null);
export const ApiContext = createContext(null);
export const CurrentChatContext = createContext(null);

export const ElevationContext = createContext(null);
export const HeadingLevelContext = createContext(null);
